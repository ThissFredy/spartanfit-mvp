import { PageLoading } from "@/components/ui/page-loading";

export default function Loading() {
  return (
    <PageLoading
      title="Preparando SpartanFit"
      subtitle="Estamos cargando módulos, estadísticas y navegación segura."
    />
  );
}
